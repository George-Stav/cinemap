# INSTRUCTIONS

At all times you will:

- maintain the `./memory.md` file with information on the context of our conversation and any changes that you make throughout the session. This file does not have to be human readable and instead should be written in a format that is easily digestible by an agent such as yourself
- maintain the `./plan.md` file with any changes that are made to the plan throughout the session. This file IS HUMAN READABLE and you are expected to keep it as such

# CODING

- when running `cargo` commands make sure to relinquish the `Cargo.lock` file in the project root directory so that the user can also try the command out
